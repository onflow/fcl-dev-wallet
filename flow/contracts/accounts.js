export const AccountsContract = `
  pub contract Accounts {
    pub struct Account {
      pub 
      
    }

    pub resource Collection {}

    pub fun getAccount(_ address: Address) {}

    init () {}
  }
`
